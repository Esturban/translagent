{
  "name": "translagent-api",
  "version": "0.0.2",
  "description": "AI Translation and Transliteration for select languages with different characters",
  "main": "src/app.ts",
  "scripts": {
    "start": "bun src/app.ts",
    "dev": "bun --watch src/app.ts",
    "build": "mkdir -p netlify/functions && bun build ./src/netlify-functions/translate.ts --outdir=netlify/functions && bun build ./src/netlify-functions/speak.ts --outdir=netlify/functions"
  },
  "dependencies": {
    "openai": "^4.0.0",
    "@sindresorhus/transliterate": "^1.6.0"
  }
}